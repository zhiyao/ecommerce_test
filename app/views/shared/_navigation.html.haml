.front-navigation.text-center
  = link_to 'All', products_path, class: 'navigation-item'
  - @categories.each do |category|
    = link_to category.name, sub_category_path(category.root), class: 'navigation-item'
